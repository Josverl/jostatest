{
  "name": "justatest",
  "version": "1.0.0",
  "description": "testing pwsh and npm ",
  "main": "index.js",
  "scripts": {
    "get-binaries": "pwsh scripts/mp-download.ps1",
    "postinstall": "pwsh scripts/copy-native.ps1",
    "build": "pwsh -command {write-host -f green 'insert typescript/babel transpiler here'}",
    "test": "mocha",
    "package-prep": "npm ci && npm prune --production && npm install vsce -g",
    "package": "vsce -package",
    "power": "pwsh -command {write-host -f green Hoi}"
  },
  "author": "Jos Verlinde",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.0",
    "node-abi": "^2.10.0",
    "nyc": "^14.1.1",
    "prebuild-install": "^5.3.0",
    "pwsh": "^0.2.0",
    "should": "^13.2.3"
  },
  "dependencies": {
    "serialport": "^7.1.5",
    "vsce": "^1.66.0"
  }
}
